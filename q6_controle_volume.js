const prompt = require('prompt-sync')();

class ControledeAudio {
    constructor(volume) {
        this.volume = volume;
        volume = 2;
    }
}
void aumentarVolume();  // Uso aqui a função void para exibir o aumentarVolume();
    while (volume < 10) {
        volume++;
    }

void diminuirVolume();  // Uso aqui também a função void para exibir o diminuirVolume();
    while (volume > 0) {
        volume--;
    }

// Implementando o get que usamos em TypeScript para retornar getVolume;
getVolume = this.volume; 
    return this.volume;
    